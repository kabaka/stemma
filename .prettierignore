dist
coverage
node_modules
prototype
*.md

# AI-DLC kit assets (agents, skills, templates, evals) carry their own upstream
# formatting and are not app source — keep them out of the app's prettier gate.
# (*.md above already covers the kit's docs; these cover its non-md assets.)
.claude/**/*.mjs
.claude/**/*.json
.claude/**/*.jsonl
.claude/**/*.mdc
.ai-dlc/

# Generated by scripts/gen-conditions.mjs — the generator owns its formatting,
# so prettier must not reformat it (that would break the CI staleness check).
src/data/conditions.ts
